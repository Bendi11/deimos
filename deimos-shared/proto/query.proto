syntax = "proto3";

package deimos;

message QueryContainersRequest {
    // Filter the returned containers by when they have been last updated
    optional int64 updated_since = 1;
}

message QueryContainersResponse {
    repeated ContainerBrief containers = 1;
}
